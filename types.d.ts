type EventDocument = {
  title: string;
  locationName: string;
  timestamp: string;
  thumbnailUrl: string;
};

type IEvent = {
  title: string;
  locationName: string;
  date: string;
  time: string;
  thumbnail: URL;
};
